export class Food {
    type: string;
    storage: number;

    constructor(_type: string, _storage: number) {
        this.type = _type;
        this.storage = _storage;
    }

    checkStock(): void {

    }
}